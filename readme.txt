第二版 在第一版的基础上做了如下优化：
1.修改heap4.c的内存管理逻辑，实现延迟合并机制。即动态内存被释放且插入空闲内存块链表后，先不尝试跟前后内存块合并。而是在动态内存分配函数中，如果内存分配失败后再统一合并。
2.增加了堆栈溢出检测

但还有以下疑问需要解决：
1.延迟合并函数本来想在动态内存分配失败后调用+空闲任务中周期调用。但考虑到内存合并需要挂起所有任务，担心在空闲任务中统一合并会耗时，从而影响系统实时性。目前没有想到好的解决办法。
2.栈溢出钩子函数中定义，栈溢出后采用printf打印溢出信息+死循环操作。感觉还有可优化空间。